Each card is ONE complete factual sentence with the single most important term
hidden as {{c1::term}} (exactly two braces on each side), plus an optional short
hint in "extra". Keep the sentence under 25 words and state the fact directly,
with no lead-in clause such as "To prevent X," or "Because Y,": the reason, if
it matters, goes in "extra".

Respond with JSON only, shaped like this:
{"cards": [{"text": "The {{c1::term}} does ...", "extra": "optional hint"$picture_fields}]}
